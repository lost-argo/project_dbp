version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: project_dbp
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: project_dbp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres